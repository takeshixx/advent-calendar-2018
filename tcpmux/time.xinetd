service time
{
        disable         = no
        type            = TCPMUXPLUS UNLISTED INTERNAL
        id              = time-stream
        socket_type     = stream
        protocol        = tcp
        user            = root
        wait            = no
}
 
